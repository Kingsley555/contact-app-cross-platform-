
(a)Pull the master branch from bitbucket and open with VS CODE or your preferred IDE
(b) ( If you want to use the zip file, unzip the folder in your desktop and open with VS CODE or your preferred IDE )

From the terminal: Navigate to the project folder and Run "npm install to install"
From the terminal Run "npm start" 
Open go to the browser and enter: "http://localhost:3000" 
---Leave the server runing
Open another terminal and run "npm run electron"

The application should be running on both the web browser and and on the local machine (Both MAC and Windows)


Please Note that you might need to install some dependencies that are compatible with your machine/computer
